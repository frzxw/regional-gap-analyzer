# Skill: Frontend (Next.js)

Frontend is READ-ONLY analytical UI.

Rules:
- No data mutation except triggering actions
- All calculations done in backend
- Map uses GeoJSON (province level only)
- Heatmap color scale must match score categories

Frontend must never fabricate data.
